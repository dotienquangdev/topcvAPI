const express = require("express");
const router = express.Router();
const controller = require("../controllers/controller.workExprerience");

// router.post("/postWorkExprerience", controller.postWorkExprerience);
// router.get("/getWorkExprerience", controller.getWorkExprerience);
// router.put("/putWorkExprerience/:id", controller.putWorkExprerience);
// router.delete("/deleteWorkExprerience/:id", controller.deleteWorkExprerience);

module.exports = router;
